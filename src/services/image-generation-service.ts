import { ImageSystemPrompt } from "@/lib/openai";

export const imageGenerationService = {
  async generateImage(data: any) {
    try {
      const systemPrompt = ImageSystemPrompt;
    } catch (error) {}
  },
};
